#rake allows you to use the command line to build functionality.

ENV["SINATRA_ENV"] ||= "development"

require_relative './config/environment'

#commands come from here 
require 'sinatra/activerecord/rake'
